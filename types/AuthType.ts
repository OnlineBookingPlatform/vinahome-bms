export interface LoginType {
    username: string,
    password: string,
}
export interface AuthStoreType {
    id: string;
    name: string;
    code: string;
    company_id: number;
    role: number;
    token: string;
}